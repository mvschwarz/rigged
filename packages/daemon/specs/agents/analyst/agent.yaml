name: analyst
version: "1.0"
description: Research analyst — deep-dive investigation and evidence gathering

defaults:
  runtime: claude-code

profiles:
  default:
    uses: []

resources:
  guidance:
    - id: role
      path: guidance/role.md

startup:
  files:
    - path: guidance/role.md
      required: true
  actions: []
