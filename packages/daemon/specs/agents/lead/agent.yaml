name: lead
version: "1.0"
description: Orchestrator lead — coordinates agents, monitors progress, bridges communication

defaults:
  runtime: claude-code

imports:
  - ref: local:../shared

profiles:
  default:
    uses:
      skills: [openrig-user, orchestration-team]
      guidance: []
      subagents: []
      hooks: []
      runtime_resources: []

resources:
  guidance:
    - id: role
      path: guidance/role.md

startup:
  files:
    - path: guidance/role.md
      required: true
  actions: []
